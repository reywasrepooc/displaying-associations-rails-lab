<h1> Artists </h1>
<ul>
  <%= @artists.each do |art|%>
    <li>
      <%link_to art.name, artist_path(art) %>
      (<%= art.song_count %> <%= "song". pluralize(art.song_count) %>)
    </li>
  <% end %>
</ul>